export async function onRequestPost(c){try{const b=await c.request.json();if(b.website)return out({success:true,submission_id:'OK'},200);if(!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(b.email||'')||String(b.name||'').length<2||String(b.review||'').length<10)return out({success:false},400);const u=c.env.BREVO_REVIEW_URL;if(!u)return out({success:false},503);const id='MES-R-'+Date.now().toString(36).toUpperCase();const p=new URLSearchParams({LASTNAME:b.name,EMAIL:b.email,CITY:b.city||'',SERVICE_INTERESTED:b.service||'',RATING:String(b.rating||'5'),REVIEW:b.review,locale:b.language||'en',html_type:'simple',email_address_check:''});const r=await fetch(u,{method:'POST',headers:{'Content-Type':'application/x-www-form-urlencoded'},body:p});return r.ok?out({success:true,submission_id:id},200):out({success:false},502)}catch(e){return out({success:false},500)}}function out(d,s){return new Response(JSON.stringify(d),{status:s,headers:{'Content-Type':'application/json','Cache-Control':'no-store'}})}
